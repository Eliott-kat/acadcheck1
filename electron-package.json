{
  "name": "academia-guard-electron",
  "version": "1.0.0",
  "main": "electron-main.js",
  "scripts": {
    "start": "electron .",
    "build": "vite build",
    "electron:dev": "npm run build && electron ."
  },
  "devDependencies": {
    "electron": "^30.0.0"
  }
}
